# 安全模型

[English](SECURITY.md) | **简体中文**

这是实验性的 macOS 进程管理与 Surge 连接防护工具，不是 VPN。

## 保护范围

系统数据过滤器使用签名身份和可验证的同用户父子关系识别 Claude 客户端。无法归属或成为孤儿的进程可能不在覆盖范围；Surge 和其他用户的普通连接被排除。退出操作不凭模糊进程名称批量终止应用。

可识别客户端被限制到已批准的 Surge 回环 TCP 入口。短时放行许可绑定运行配置、签名有效的 Surge 监听进程、预期出口探针、过滤器代次、启动／控制会话及新鲜挑战值。过期或格式错误的证据不会放行受保护连接；顺序编号防止迟到的旧许可覆盖较新的阻断请求。

## 限制

- 数据过滤判定不能证明远端从未收到任何 IP 数据包。
- 周期性配置／出口检查不是每次请求的密码学路径证明。
- 配置变动被发现前存在有限观察窗口。
- 过滤器崩溃、停用、系统升级和重启恢复需要专项实机验证。窗口正在运行不等于过滤生效。
- 签名身份缓存和后代跟踪有边界。未知归属会被明确披露，不会显示成全覆盖保护。
- 源码公开、签名和公证不等于全面安全审计。

## 数据与问题反馈

不需要 VPS 私钥、账户秘密、Cookie 或聊天内容。活动 Surge 配置仅在内存处理；持久设置仅含非秘密入口、预期出口、策略名称及配置摘要。不要提交真实配置、日志、凭据、权限配置文件／私有签名导出或用户目录。

通过仓库当前可用的安全报告渠道私下反馈问题，不要在公开 issue 中附上凭据或完整个人日志。
