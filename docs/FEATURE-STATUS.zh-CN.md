# 验证状态 — 0.4.0 / build 12.0

[English](FEATURE-STATUS.md) | **简体中文**

## 已实现

可识别进程的发现与确认退出、只读 IPv6、可选的 Surge 路径检查与系统过滤、安全通知、双语界面和手册、原生 Xcode 目标、Dock／菜单栏入口，以及独立 Preview。

## 已完成的本地验证

- 主程序 222 项、Preview 220 项离线断言通过。两组包含共享覆盖，不能相加。
- Intel／Apple Silicon 主程序和过滤器构建、严格 ad-hoc 签名检查及原生 Xcode 构建通过。
- 实际运行的离线 Preview 已检查两种语言：切换语言保留示例阻断状态、取消退出保留示例进程、配置输入框可见、新图标出现在确认框。
- README 截图使用该 Preview 的示例数据，不是真实过滤证据。
- 独立源码发布审查覆盖冻结源码／历史、图片和精确本地产物，不等于生产安全认证。

## 尚未验证

生产环境授权／激活、真实客户端流量及所有包装器／后代、通知投递、重启首包、过滤器崩溃恢复、物理断网和全部 IPv6 切换场景。

## 分发状态

这是实验性源码候选，不是正式签名安装包。本轮未安装或启用生产过滤器，未改变用户现有代理或客户端。安排隔离实机验收前，请阅读[安全模型](../SECURITY.zh-CN.md)和[使用说明](UserGuide.md)。
